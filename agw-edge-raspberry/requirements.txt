# ============================================================
#  requirements.txt — AGW Edge Controller (Raspberry Pi)
# ============================================================
#  Destino: Debian 13 Trixie · Python 3.13 · aarch64
#
#  ⚠️ RANGOS, NO PINES EXACTOS. Motivo: las versiones clavadas
#  originales (pydantic 2.6.0, fastapi 0.110) no publican wheel
#  para cp313/aarch64. pip intentaba compilar pydantic-core desde
#  Rust y fallaba: su generate_self_schema.py usa la firma antigua
#  de ForwardRef._evaluate(), incompatible con Python 3.13.
#
#  Con rangos, pip resuelve a una version que SI trae wheel
#  precompilado y la instalacion baja de ~15 min a ~40 s.
# ============================================================

# ── MQTT asincrono ────────────────────────────────────────
# aiomqtt 2.x, sucesor de asyncio-mqtt. El codigo usa el
# parametro `identifier=`, que solo existe a partir de 2.0
# (en asyncio-mqtt se llamaba `client_id=`).
aiomqtt>=2.0,<3

# ── Cliente HTTP hacia la Cloud API ───────────────────────
httpx>=0.27,<1

# ── Logging estructurado ──────────────────────────────────
structlog>=24.1

# ── YAML (config.yaml, rules.yaml) ────────────────────────
PyYAML>=6.0

# ── Validacion de configuracion y reglas ──────────────────
# 2.10+ garantiza wheel para Python 3.13 en aarch64
pydantic>=2.10,<3
pydantic-settings>=2.6,<3

# ── Buffer local SQLite asincrono ─────────────────────────
aiosqlite>=0.20

# ── Servidor de salud / metricas ──────────────────────────
# uvicorn SIN [standard]: ese extra arrastra uvloop, httptools
# y watchfiles, que en ARM pueden requerir compilacion. Para
# un endpoint de diagnostico el loop estandar sobra.
fastapi>=0.115,<1
uvicorn>=0.30,<1
